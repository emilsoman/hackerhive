<a href="<%= root_path%>" class="brand">
  <span>
    <%= t(:app_title) %><sub>β</sub>
  </span>
</a>
<ul class="nav pull-right">
  <% if user_signed_in? %>
    <li>
      <%= link_to "Logout", destroy_user_session_path, method: :delete %>
    </li>
  <% end %>
</ul>
